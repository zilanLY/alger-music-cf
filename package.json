{
  "name": "alger-music-cf",
  "version": "1.0.0",
  "description": "Alger Music - Cloudflare Workers (Meting API + Frontend)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:frontend": "cd web && npm install && npm run build",
    "deploy:all": "npm run build:frontend && npm run deploy"
  },
  "dependencies": {
    "@meting/core": "^1.6.0",
    "aes-js": "^3.1.2",
    "hash.js": "^1.1.7",
    "hono": "^4.10.3",
    "lru-cache": "^11.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}